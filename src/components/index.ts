/**
 * @version 1.0.0
 * @description Aquí se exportan todos los componentes para que puedan ser importados desde un solo archivo.
 * @author Juan Manuel Cuellar Cardoso
 */
import SideBar from './SideBar';
import PlayerFooter from './Player/Footer';
import Player from './Player/Player';
import Chat from './Chat';
import NavBar from './NavBar';
import PlayerContainer from './Player';
import Button from './Button';
import Avatar from './Avatar';

export { SideBar, PlayerFooter, Player, Chat, NavBar, PlayerContainer, Button, Avatar };
